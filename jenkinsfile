pipeline {
    agent any
    stages {
        stage("Checkout") {
            steps {
             echo 'Get the code from Jenkinsfile'
            }
        }
        stage("Compile") {
            steps {
             echo 'Compile stage when setup'
            }
        }
        stage("Unit test") {
            steps {
                echo 'Test stage when setup'
            }
        }
    }
}