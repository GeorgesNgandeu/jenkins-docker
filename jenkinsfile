pipeline {
    agent any
    stages {
        stage("Checkout") {
            steps {
             git url: 'https://github.com/GeorgesNgandeu/jenkins-docker.git'
            }
        }
        stage("Compile") {
            steps {
             echo 'Compile stage when setup'
            }
        }
        stage("Unit test") {
            steps {
                echo 'Test stage when setup'
            }
        }
    }
}